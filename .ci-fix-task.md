Fix 6 known issues from docs/architecture/KNOWN_ISSUES.md: (1) Dashboard STATUS polling every 2s instead of relying on 4-byte notify, (2) Fleet Overview RSSI 1s update, (3) Scanner restart scan on return from DeviceScreen, (4) Roster filter out EDs already in firmware roster, (5) HA show 'No HA pair' instead of waiting forever, (6) Throughput show N/A when tp_Bps=0 in non-TP mode
